
  # Freelance Web Design Website

  This is a code bundle for Freelance Web Design Website.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
